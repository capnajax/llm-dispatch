import { SpawnOptions } from 'node:child_process';
import { Writable } from 'node:stream';
interface ShellOptions extends Partial<SpawnOptions> {
    echo?: boolean | {
        stdout?: boolean | Writable | null;
        stderr?: boolean | Writable | null;
    };
    shell?: boolean | string;
}
type SpawnOptionsTemplateFunction = (strings: TemplateStringsArray, ...values: any[]) => Promise<string>;
declare function $(strings: TemplateStringsArray, ...values: any[]): Promise<string>;
declare function $(options: ShellOptions): SpawnOptionsTemplateFunction;
export default $;
